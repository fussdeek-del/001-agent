# render.yaml
services:
  - type: web
    name: slack-ai-agent
    env: node
    plan: free 
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: slack-db # Must match your Render DB name
          property: connectionString
